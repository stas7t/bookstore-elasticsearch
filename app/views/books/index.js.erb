$('#books').append("<%= escape_javascript(render 'books')%>");
$('#paginator').html("<%= escape_javascript(link_to_next_page(@books,
'LOAD MORE', remote: true, id: 'view_more'))%>");
if (!$('#view_more').length) { $('#paginator').remove(); }