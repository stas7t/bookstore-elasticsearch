%h1.text-center Listing categories

%table.table
  %thead
    %tr
      %th
        %span.in-grey-600 Category
      %th

  %tbody
    - @categories.each do |category|
      %tr
        %td.title= category.name
        %td
          = link_to 'Edit', edit_category_path(category), class: "font-16 in-gold-500"
          %span.font-16.in-gold-500 
            \|
          = link_to 'Delete', category, method: :delete, data: { confirm: 'Are you sure?' }, class: "font-16 in-gold-500"

.text-center
  = link_to 'Create', new_category_path, class: "btn btn-default mb-20"
